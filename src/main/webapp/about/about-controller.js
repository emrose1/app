Application.Controllers.controller( 'about', ['$rootScope', '$scope', '$location', 'alerts',
    function ($rootScope, $scope, $location, alerts) {

    //console.log('called');
    /*var apiLoadedSuccess = function () {
        console.log('loaded success');
        $location.path('/login');
        $scope.$apply();
    };

    $rootScope.$on('EventLoaded', apiLoadedSuccess);*/
}])

;